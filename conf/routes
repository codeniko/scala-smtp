# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


POST   /api/smtp                                  controllers.SmtpController.sendMail()

#### Build Info ####
GET    /build                                     controllers.BuildInfoController.buildInfo()

###### Debugging routes ########
GET    /debug/config                              controllers.DebugController.config()

###### Healthcheck and Status #######
GET    /healthcheck                               controllers.HealthCheckController.healthCheck
GET    /status                                    controllers.StatusController.status
